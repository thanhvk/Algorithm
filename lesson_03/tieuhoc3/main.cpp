#include <iostream>
#include <fstream>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include <ncurses.h>
#include <string>

using namespace std;

int main()
{
    int ntest, itest, s;
    int m[5];
    m[0] = 1;
    m[1] = 5;
    m[2] = 10;
    m[3] = 20;
    m[4] = 50;

    freopen("tieuhoc1.inp", "r", stdin);

    scanf("%d", &ntest);

    for (int itest = 0; itest < ntest; itest++) {
        scanf("%d", &s);
    }

    getchar();
    return 0;
}
